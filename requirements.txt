# Photo Quality Analyzer - Phase 1 Dependencies
# Python 3.11+ required

# Core image processing
opencv-python>=4.8.0,<5.0.0       # Computer vision library for Laplacian and histogram
numpy>=1.24.0,<2.0.0               # Array operations and numerical computing
Pillow>=10.0.0,<11.0.0             # Image loading for additional format support

# Google Photos Integration (Feature 1.5)
google-auth>=2.23.0,<3.0.0         # Google OAuth authentication
google-auth-oauthlib>=1.1.0,<2.0.0 # OAuth flow helpers
google-auth-httplib2>=0.1.1,<1.0.0 # HTTP transport for auth
google-api-python-client>=2.100.0,<3.0.0  # Google Photos API client
cryptography>=41.0.0,<42.0.0       # Token encryption (Fernet)
keyring>=24.0.0,<25.0.0            # Secure key storage in OS keyring
requests>=2.31.0,<3.0.0            # HTTP requests for downloads

# Web server and API framework
fastapi>=0.104.0,<1.0.0            # Modern async API framework
uvicorn[standard]>=0.24.0,<1.0.0   # ASGI server with auto-reload
python-multipart>=0.0.6,<1.0.0     # Form data parsing for FastAPI
flask>=3.0.0,<4.0.0                # Lightweight WSGI web framework (sync server)
flask-cors>=4.0.0,<5.0.0           # CORS support for web viewer

# Testing dependencies
pytest>=7.4.0,<8.0.0               # Unit testing framework
pytest-cov>=4.1.0,<5.0.0           # Code coverage for tests

# Development dependencies (optional)
# black>=23.0.0                    # Code formatting
# mypy>=1.5.0                      # Type checking
# pylint>=2.17.0                   # Code linting
